# 群星日程 · 环境变量示例
# 复制为 .env.local 后填写（.env.local 已被 .gitignore 忽略，不会提交）

# 【服务端变量 · 推荐】真实 API Key 只存服务端，绝不进浏览器产物。
# 同一组变量两处生效：
#   · Vercel 部署：Dashboard → Settings → Environment Variables 设置
#   · 本地开发：写进 .env.local，npm run dev 时由 Vite 中间件（vite-plugin-dev-api.ts）
#     加载并托管 /api/*，与 Vercel 生产完全同构，零配置开箱即用
# UPSTREAM_API_KEY=sk-...                     # 必需，DeepSeek / Moonshot / OpenAI 等兼容接口
# UPSTREAM_BASE_URL=https://api.deepseek.com/v1  # 可选，默认 https://api.deepseek.com/v1
# UPSTREAM_MODEL=deepseek-chat                # 可选，默认 deepseek-chat
# ALLOWED_ORIGINS=https://your-app.vercel.app # 可选，跨域白名单（同源部署留空即可）

# 【GitHub Pages 部署】官方 LLM 代理地址（Cloudflare Worker，见 workers/llm-proxy/）
# 设置后访问者免 Key 开箱即用；真实密钥只存在于 Worker secrets
# VITE_PROXY_URL=https://qunxing-llm-proxy.<your-subdomain>.workers.dev

# 【遗留 · 不推荐】自备 Key 直连（BYOK 环境变量方式）——Key 会打包进浏览器产物！
# 仅在私有部署/本地调试时可接受；优先使用上面的服务端变量
# VITE_LLM_BASE_URL=https://api.openai.com/v1
# VITE_LLM_API_KEY=sk-...
# VITE_LLM_MODEL=gpt-4o-mini

# 【可选】Keyed 图像 API 代理（默认不需要：人物画像走免费免 Key 的 pollinations.ai）
# 服务端变量（Vercel Dashboard 或本地 .env.local）：
# IMAGE_API_KEY=sk-...                        # 设置后 /api/image 可用
# IMAGE_BASE_URL=https://api.openai.com/v1    # 可选
# IMAGE_MODEL=dall-e-3                        # 可选
# 前端变量（设置后画像改走服务端代理）：
# VITE_IMAGE_BASE_URL=/api/image
# VITE_IMAGE_MODEL=dall-e-3

# 【可选】语录检索站点覆盖（默认 https://cn.bing.com，镜像或本地测试用）
# 服务端变量：BING_BASE=https://cn.bing.com

# 【谨慎】宽松语录兜底（仅演示/备用，生产请勿开启）：检索全失败时凭大模型知识
# 生成语录（未经联网核验，UI 会如实标注「未经核验」）；默认关闭——关闭时检索
# 失败会直接拒绝生成，绝不编造语录
# VITE_QUOTE_FALLBACK=llm
